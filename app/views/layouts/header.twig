<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}YouEvent{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50">
<div class="min-h-screen">
    <nav class="bg-white shadow">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between h-16">
                <div class="flex">
                    <a href="home" class="flex items-center">
                        <span class="text-xl font-black">You<span class="text-orange-500">Event</span></span>
                    </a>
                </div>
                <ul class="flex items-center gap-4">
                    <li>
                        <a href="home" class="flex items-center">
                            Home
                        </a>
                    </li>
                    <li>
                        <a href="events" class="flex items-center">
                            Events
                        </a>
                    </li>

                </ul>
                <div class="flex items-center gap-4">
                    <a href="{{ path('profile_tickets') }}" class="text-gray-600 hover:text-gray-900">Mes billets</a>
                    <a href="{{ path('profile_settings') }}" class="{% block nav_settings_class %}text-gray-600 hover:text-gray-900{% endblock %}">Paramètres</a>
                    <a href="{{ path('logout') }}" class="text-gray-600 hover:text-gray-900">Déconnexion</a>
                </div>
            </div>
        </div>
    </nav>

    {% block content %}{% endblock %}
</div>
</body>
</html>